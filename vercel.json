{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd perf-ui && npm install",
  "buildCommand": "cd perf-ui && npm run build",
  "outputDirectory": "perf-ui/dist",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
